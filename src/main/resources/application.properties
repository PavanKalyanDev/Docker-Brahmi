# ------------------------
# Application Info
# ------------------------
spring.application.name=InternStudentService

# ------------------------
# Database Configuration (Docker)
# ------------------------
spring.datasource.url=jdbc:mysql://mysql-db:3306/studentdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Pspk#@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ------------------------
# JPA & Hibernate Settings
# ------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ------------------------
# Server Port
# ------------------------
server.port=9090

# ------------------------
# File Upload (Docker volume mounted at /app/uploads)
# ------------------------
file.upload-dir=/app/uploads
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=50MB

# ------------------------
# Mail Configuration (Gmail SMTP)
# ------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ppspk7263@gmail.com
spring.mail.password=ljnwwuyklpvyjgpi
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
